# Proposal, Repo, and Research Action Ledger

Updated: 2026-04-13

| Topic | Proposal commitment | Current repo truth | Newer-research temptation | Chosen action for this run | Evidence | Claim impact |
| --- | --- | --- | --- | --- | --- | --- |
| Generator training | Fine-tune BART on XSum. | `train` now runs a bounded BART fine-tuning pass and exports the best checkpoint. | Swap to a newer summarizer. | Completed with the proposal-faithful BART lane; keep newer summarizers in future work only. | `docs/guidelines/FINAL_PROJECT_PROPOSAL.md`, `artifacts/models/training_manifest.json` | Final story centers the fine-tuned BART lane. |
| Baseline comparator | Keep a public baseline. | Public `facebook/bart-large-xsum` config already exists. | Drop the baseline once fine-tuned model exists. | Preserve the public baseline as explicit comparator. | `configs/model/bart_xsum_public.yaml`, ExecPlan decision log | Supports honest before/after comparison. |
| Factuality signals | Combine entailment-style and FactCC-style signals with likelihood. | Repo now executes SummaC-style, FactCC-style, and entity-support scoring on refreshed HF candidates. | Add many new metrics before core truth is fixed. | Keep the current signal set for the main reranker and add MiniCheck only as a bounded subset evaluator. | `src/factuality_rerank_xsum/scoring/`, `artifacts/scores/minicheck/stage_summary.json` | Preserves methodology without overscoping the main study. |
| Iterative refinement | Implement one refinement based on observed failures. | Entity-support is now the explicit refinement lane and improves the named ablation slightly. | Replace with a larger redesign. | Close the refinement story with entity-support augmentation and keep stronger guardrails as future work. | `outputs/final/ablation_metrics.csv`, `configs/rerank/final_selection.yaml` | Enables an honest “v1 vs refined” narrative. |
| Audit method | Annotate a stratified sample to validate metrics. | Final audit now contains 24 stratified completed rows with explicit Codex / AI-assisted provenance. | Treat heuristic labels as “manual enough.” | Keep the two-phase template/completion workflow and use exact provenance wording. | `src/factuality_rerank_xsum/audit/stage.py`, `artifacts/audit/manual_audit_completed.csv` | Must avoid false human-label claims. |
| Final scale claims | Report bounded results honestly. | Active artifacts, final outputs, and package now agree on the bounded run. | Write as if full-scale benchmark claims already hold. | Keep the bounded-scale wording and cite the exact executed split sizes and subset evaluator scope. | `artifacts/validation/artifact_truth_report.json`, `docs/RESULTS_SUMMARY.md` | Claim ceilings remain conservative even though the refresh completed. |
