# PLAN

This file is superseded as a primary authority.

Use these files instead:

1. `REPORT.md` for the canonical current-state handoff.
2. `docs/RUNBOOK.md` for the canonical operator flow.
3. `docs/planning/CODEX_EXECUTION_REQUIREMENTS.md` for the checked-in
   execution checklist for future zero-context Codex sessions.
4. `docs/RESULTS_SUMMARY.md` for the current artifact-backed metrics snapshot.
5. `docs/CLAIMS_SAFE_TO_WRITE.md` for claim ceilings.

Keep this file only as a compatibility pointer for older references and prompt
surfaces. Do not treat it as a co-equal authority with `REPORT.md`.
